apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core infrastructure components
  - ingress-nginx.yaml
  - cert-manager.yaml
  - metrics-server.yaml
  # Add more as needed
